---
name: merge-pr
description: Merge a GitHub pull request — local checkout, tests, validation audit, then squash-merge
---

Invoke the `merge-pr` skill. PR number is in `$ARGUMENTS` (or detect from the current branch if empty).
